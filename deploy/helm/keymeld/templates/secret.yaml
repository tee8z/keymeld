{{- if .Values.kms.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "keymeld.fullname" . }}-kms
  labels:
    {{- include "keymeld.labels" . | nindent 4 }}
type: Opaque
stringData:
  keyArn: {{ .Values.kms.keyArn | default "REPLACE_WITH_KMS_KEY_ARN" | quote }}
{{- end }}
