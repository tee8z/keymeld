apiVersion: v1
kind: Service
metadata:
  name: {{ include "keymeld.fullname" . }}
  labels:
    {{- include "keymeld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keymeld.serviceSelectorLabels" . | nindent 4 }}
